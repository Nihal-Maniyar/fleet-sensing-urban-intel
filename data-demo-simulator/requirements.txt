# Data/Test/Demo Simulator requirements
# Core simulator has ZERO mandatory dependencies (runs on Python 3.10+ standard library)
# Optional dependencies below enable live MQTT publishing and automated testing:

paho-mqtt>=1.6.1,<3.0.0
pytest>=7.0.0
